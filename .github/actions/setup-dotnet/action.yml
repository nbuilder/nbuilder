name: 'Setup .NET and Restore'
description: 'Setup .NET from global.json and restore dependencies'

runs:
  using: composite
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        global-json-file: global.json
        
    - name: Restore dependencies
      shell: bash
      run: dotnet restore
